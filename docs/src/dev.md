# Developing Legate.jl

There are two primary ways to develop `Legate.jl`:
- Clone the git repo and only work with `Legate.jl`
- Add `Legate.jl` to another environment to test functionality with other packages

